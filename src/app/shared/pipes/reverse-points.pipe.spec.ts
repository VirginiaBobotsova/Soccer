import { ReversePointsPipe } from './reverse-points.pipe';

describe('ReversePointsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReversePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
